# Example Target Repository

This fixture is a miniature Python repository used to prove the safe project
improvement workflow can be adopted by another repo.

The represented workflow is:

```text
inspect -> characterize -> verify setup -> audit -> backlog -> one patch -> verify
```

It is intentionally small. The safe project improvement system is present only
as a development/support skill, not as runtime application code.
